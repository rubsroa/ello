import { requireUser } from "@/lib/auth/authorization";
import { db } from "@/lib/db/client";
import { SettingsForm } from "@/components/admin/settings-form";
import { OpeningHoursManager } from "@/components/admin/opening-hours-manager";
export const dynamic = "force-dynamic";
export default async function SettingsPage() { await requireUser(["ADMIN"]); const [record, storedHours] = await Promise.all([db.siteSettings.findUnique({ where: { key: "booking" } }), db.openingHours.findMany({ orderBy: { weekday: "asc" } })]); const data = record?.value && typeof record.value === "object" && !Array.isArray(record.value) ? record.value as Record<string, unknown> : {}; const initial = { paymentsEnabled: data.paymentsEnabled === true, minimumNoticeMinutes: typeof data.minimumNoticeMinutes === "number" ? data.minimumNoticeMinutes : 120, maximumAdvanceDays: typeof data.maximumAdvanceDays === "number" ? data.maximumAdvanceDays : 90, timeZone: "Europe/Zurich" as const }; const hours = Array.from({ length: 7 }, (_, weekday) => storedHours.find((day) => day.weekday === weekday) ?? { weekday, closed: true, opensAtMinutes: 9 * 60, closesAtMinutes: 18 * 60 }); return <><p className="eyebrow text-brass">Configuration</p><h1 className="mt-3 font-display text-4xl font-light tracking-[-.04em]">Réglages</h1><SettingsForm initial={initial} /><OpeningHoursManager initialDays={hours.map(({ weekday, closed, opensAtMinutes, closesAtMinutes }) => ({ weekday, closed, opensAtMinutes, closesAtMinutes }))} /></>; }
